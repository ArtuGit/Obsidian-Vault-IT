### **🧱** 

### **Architecture & Code Quality**

- Follows scalable component architecture (e.g., Atomic Design, Feature-Sliced Design)
    
- Implements code-splitting & lazy loading
    
- Uses TypeScript with strict type checking
    
- Enforces consistent linting (ESLint, Prettier, Stylelint)
    
- Uses modern state management patterns (e.g., Zustand, Jotai, Redux Toolkit)
    
- Handles error boundaries & fallback UI
    

---

### **⚙️** 

### **Frameworks & Tooling**

- Uses modern frameworks (Next.js 14+, Remix, Astro)
    
- Integrates [[Server-side rendering (SSR)]], ISR, or SSG as needed
    
- Configures and optimizes bundlers (e.g., Vite, Webpack 5+)
    
- Implements module federation or micro-frontend patterns if needed
    
- Uses monorepo tools (e.g., Turborepo, Nx) if managing multiple packages
    

---

### **🎨** 

### **Styling & UI Systems**

- Uses a design system or component library (e.g., Radix UI, shadcn/ui, Tailwind CSS)
    
- Implements dark/light theme support
    
- Ensures full responsiveness (mobile-first + fluid layout)
    
- Adheres to accessibility (WCAG 2.2 compliance, semantic HTML)
    
- Uses CSS-in-JS, Tailwind, or CSS Modules consistently
    

---

### **🔐** 

### **Security Best Practices**

- Validates and sanitizes all user input
    
- Prevents XSS, CSRF, and clickjacking
    
- Uses HTTPS and secure cookies
    
- Ensures secure localStorage/sessionStorage use
    
- Implements auth patterns securely (e.g., JWT rotation, OAuth PKCE)
    

---

### **🚀** 

### **Performance Optimization**

- Uses Lighthouse or Web Vitals to audit performance
    
- Lazy loads images (native <img loading="lazy"> or IntersectionObserver)
    
- Implements image optimization (AVIF/WebP, responsive sizes)
    
- Minimizes CLS, LCP, FCP metrics
    
- Uses SWR, React Query, or caching for data fetching
    

---

### **🔍** 

### **Testing & QA**

- Writes unit and integration tests (Jest, Vitest, RTL)
    
- Implements E2E tests (Playwright, Cypress)
    
- Adds visual regression testing (Chromatic, Percy)
    
- Uses test coverage reporting tools
    
- Mocks APIs and external services in test environments
    

---

### **🧪** 

### **DevOps & CI/CD**

- Uses CI pipelines (GitHub Actions, GitLab CI, CircleCI)
    
- Automates linting, formatting, and testing in CI
    
- Supports preview builds on PRs
    
- Uses feature flags or A/B testing platforms (LaunchDarkly, Split.io)
    
- Tracks errors/logs with tools like Sentry, LogRocket, Datadog
    

---

### **📦** 

### **Dependencies & Package Management**

- Uses modern package managers (pnpm, Bun, npm 10+)
    
- Keeps dependencies up-to-date and audits for vulnerabilities
    
- Avoids over-reliance on heavy libraries or unnecessary packages
    
- Uses bundler tree-shaking effectively
    
- Leverages ESM-first packages where possible
    

---

### **📈** 

### **Monitoring & Analytics**

- Integrates Web Vitals reporting (e.g., Google Analytics 4, Vercel Analytics)
    
- Implements custom logging and client-side telemetry
    
- Uses RUM (real user monitoring) tools where applicable
    
- Defines custom events for product metrics
    

---

### **📚** 

### **Documentation & Collaboration**

- Documents components and utilities (e.g., Storybook, Ladle)
    
- Maintains clear README and project structure
    
- Uses conventional commits and semantic versioning
    
- Reviews PRs with code quality, clarity, and long-term maintainability in mind
    
- Mentors junior developers and leads code reviews effectively
    

---

### **🌍** 

### **Internationalization (i18n)**

- Uses a modern i18n library (e.g., next-intl, react-i18next)
    
- Supports RTL languages and local date/number formatting
    
- Loads locale data efficiently
    
- Ensures translation keys are easy to manage and audit